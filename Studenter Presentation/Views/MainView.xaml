<Window x:Class="Studenter.Presentation.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Height="550"
        Title="Studenter"
        Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="28.6" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Menu IsMainMenu="True" Padding="5">
            <MenuItem Header="Studenten">
                <MenuItem Header="Student immatrikulieren" />
                <MenuItem Header="Studenten suchen" />
            </MenuItem>
            <MenuItem Header="Veranstaltungen">
                <MenuItem Header="Veranstaltung erstellen" />
                <MenuItem Header="Veranstaltungen suchen" />
            </MenuItem>
            <MenuItem Header="Hilfe">
                <MenuItem Click="OnAboutClick" Header="Über" />
                <Separator />
                <MenuItem Header="Beenden" />
            </MenuItem>
        </Menu>

    </Grid>
</Window>